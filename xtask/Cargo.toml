[package]
name = "xtask"
version = "0.0.0"
edition.workspace = true
publish = false
description = "Developer workflow runner for the Aeon workspace (cargo xtask ...)."

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
